# Voicednut example environment variables
# Copy this file to .env and fill in values before running locally.

# Server
PORT=3000
SERVER=localhost:3000  # public host (used to construct Twilio webhook URLs in this project)
NODE_ENV=development

# Twilio
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
FROM_NUMBER=+15551234567

# Deepgram (used for live transcription)
DEEPGRAM_API_KEY=dg_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ElevenLabs (if used)
ELEVENLABS_API_KEY=elevenlabs_xxx
ELEVENLABS_AGENT_ID=agent-uuid-or-id

# OpenRouter / OpenAI (used by GPT service)
OPENROUTER_API_KEY=or_xxx
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# Telegram bot (grammY)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=987654321

# Optional: file-based DB path
SQLITE_FILE=./db/data.db

# Notes:
# - Keep .env out of source control.
# - For production, use a secrets manager rather than .env files.
